import { Box, Typography } from "@mui/material";

export const Calendar = () => {
  return (
    <Box
      sx={{
        // width: 1,
        border: 1,
        borderColor: "red",
        height: 1500,
      }}
    >
      Calendar
      <Typography variant="h1">სექტემბერი</Typography>
    </Box>
  );
};
