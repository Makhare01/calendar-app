import { Box, Typography } from "@mui/material";

export const Header = () => {
  return (
    <Box
      sx={{
        flex: "0 1",
        p: 3,
      }}
    >
      <Typography variant="body1">Header</Typography>
    </Box>
  );
};
