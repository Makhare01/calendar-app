import { AppLayout } from "./components/app-layout";
import { Calendar } from "./components/calendar";

export const App = () => {
  return (
    <AppLayout>
      <Calendar />
    </AppLayout>
  );
};
